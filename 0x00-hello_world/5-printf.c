#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Program to print some words with printf'
 * Return: Always 0 (To stop the program)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
