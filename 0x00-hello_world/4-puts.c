#include <stdio.h>

/**
 * main - Entry point
 * Description: 'to print the word, programming is like...'
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
