#include <stdio.h>

/**
 *  main - prints the number of arguenments passed to it
 *  @argc: arguemnet count
 *  @argv: pointer arg variable
 *  Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
